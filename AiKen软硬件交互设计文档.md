# AiKen 110 概念相机 — 软硬件交互设计文档

> 版本：0.1.0 | 项目：AiKen 便携胶片模拟相机（SC140HS）  
> 形态参考：Rollei A110 套筒结构，110 胶卷机横向开合

---

## 一、产品概述

AiKen 是一款仿胶卷体验的数码相机概念产品，硬件形态为**横向开合 110 胶卷机**。左壳为主体（快门键 + 品牌标识），右壳为短盖（取景器 + 状态屏 + Type-C + 复位键），内芯为贯通套筒，开闸后露出镜头。通过米家 App 连接，实现风格选择、拍摄、冲扫、分享的完整胶卷体验闭环。

---

## 二、硬件模型

### 2.1 机身结构

| 部件 | 说明 |
|------|------|
| **内芯套筒** | 贯通左右壳，固定不动。内含镜头组、闪光灯窗、过片就绪指示灯。开闸后露出中段 |
| **左壳** | 主体。含快门键（橙色大按键 + 金属座圈）、品牌铭牌「AiKen / DIGITAL 110」 |
| **右壳** | 短盖。含旁轴取景器（前窗 + 后目镜）、两位数码状态屏、复位键 RESET、Type-C 接口、挂绳柱 |
| **橙色饰条** | 贴内芯前脸下段，开闸可见、关闸隐藏 |

### 2.2 关键部件详情

**镜头组**（5 层结构）
- Housing → Bezel → Barrel → Glass → 镀膜反光（紫蓝色）
- 参数铭牌：AiKen 1:2.4 / 3.5mm
- 快门叶片：拍摄时黑色闪动模拟曝光
- 关闸状态镜头完全不可见

**闪光灯窗**
- 位于镜头正上方
- 拍摄时由闪光灯窗发出白光（PointLight），镜头本身不发光
- 内置点光源模拟闪光效果

**旁轴取景器**
- 前窗 + 后目镜同轴设计
- 位于右壳上部
- 前后均有玻璃面板，模拟真实光学取景器

**状态显示屏**
- 两位数码屏，位于机背取景器正下方
- 显示内容：
  - 关机：不显示
  - 开机：剩余可拍张数（01-36）
  - 充电中：CH
  - 低电量：LO 与数字按 1 秒交替闪烁（阈值 ≤15%）

**过片就绪指示灯**
- 位于闪光灯窗右侧
- 绿色 = 已过片，可拍摄
- 暗红色 = 待过片

**复位键 RESET**
- 底部凹陷小键
- 短按：配对提示（屏闪 + 提示音）
- 长按 2.5 秒（演示时序，实际 PRD 为 7 秒）：硬件复位

**Type-C 接口**
- 右侧面，仅充电功能
- 点击模拟插拔电源，屏显 CH 表示充电中

**快门键**
- 左壳顶部靠外侧
- 橙色大按键 + 金属座圈
- 关闸时锁定（几乎按不动）
- 传输中锁定

---

## 三、硬件交互

### 3.1 开闸 / 关闸

| 操作 | 触发方式 | 效果 |
|------|----------|------|
| **开闸** | 点击机身任意部位（非快门/复位/Type-C） | 左右壳向外滑开 0.56 单位 → 开机 → 自动过片上弦 → 连接米家 |
| **关闸** | 再次点击机身 | 左右壳合拢 → 启动 6 秒自动关机计时（PRD 为 30 秒）→ 超时后关机 |

- 开闸 = 开机 + 过片（cocked = true）
- 关闸 = 镜头隐藏，机身浑然一体（仅留发丝缝 0.004 单位）
- 剩余张数为 0 时开关闸不再上弦

### 3.2 快门拍摄

| 条件 | 行为 |
|------|------|
| **开闸 + 已上弦 + 剩余 > 0** | 张数 -1 → 取消上弦 → 快门叶片闪动 → 闪光 → 机械音效 → 上报 onShot |
| **关闸** | 快门锁定，仅下沉 15% 行程 + 锁定音效 |
| **传输中** | 快门锁定，仅下沉 30% 行程 + 锁定音效 |
| **未过片 / 剩余为 0** | 可按下但行程减半 + 沉闷反馈音效 |

### 3.3 过片机制

- 无独立过片扳手
- **开闸即自动上弦**（cocked = true）
- 每次拍摄后 cocked = false，需重新开闸或复位才能再次拍摄
- 模拟单张机械过片体验

### 3.4 硬件复位

- 仅在「开闸 + 开机」状态下可触发
- 长按 RESET 键 2.5 秒（演示）→ 屏显序列动画（-- 与空格交替 6 拍）→ 重置 count = 36 → 上报 onReset
- 短按 RESET 键 = 配对提示（屏闪 + 提示音）

### 3.5 充电模拟

- 点击 Type-C 接口切换充电状态
- 充电时屏显 CH，状态上报 charging = true

### 3.6 视角操作

- 拖拽旋转（水平自由、垂直 ±1.25 rad）
- 滚轮 / 双指缩放（0.72× ~ 2.6×）
- 键盘 Enter / 空格 = 开合闸

### 3.7 音效

- WebAudio 合成，无需外部素材
- 音效类型：开合滑动、快门、锁定、沉闷反馈、过片提示音

---

## 四、硬件状态

### 4.1 内部状态机

```
count: 36          // 剩余可拍张数
cocked: false      // 是否已过片上弦
power: false       // 是否开机
resetting: false   // 是否正在复位
battery: 78        // 电量百分比（可通过 ?battery=12 注入低电场景）
charging: false    // 是否充电中
gateOpen: false    // 开闸状态（由 targetGateRef > 0.5 判定）
```

### 4.2 状态上报（硬件 → 软件）

硬件通过 `onHardware` 回调实时上报：

```js
{
  power: boolean,      // 开机状态
  gateOpen: boolean,   // 开闸状态
  count: number,       // 剩余张数
  cocked: boolean,     // 过片状态
  battery: number,     // 电量百分比（整数）
  charging: boolean    // 充电状态
}
```

---

## 五、软件页面流程

### 5.1 整体架构

- **底部 Tab**：相机 / 暗房 / 设置
- **二级路由**：风格列表 → 风格详情 / 传输 / 胶卷详情 / 照片查看 / 拼贴 / 打印机配对 / 打印机详情
- **布局**：左栏 3D 硬件模型 + 中栏交互说明面板 + 右栏手机模拟弹窗

### 5.2 完整用户路径

```
[相机页]
  │
  ├─ 开闸 → 开机 → 自动连接米家（1.3 秒）
  │
  ├─ 选择风格 → [风格列表] → 点卡片 → [风格详情] → 选用
  │     ├─ 5 个首发风格：温暖午后 / 人像经典 / 日系蓝调 / 柏林之冬 / 经典黑白
  │     └─ 1 个运营解锁风格：霓虹之夜（解锁码 AIKEN110）
  │
  ├─ 拍摄（按硬件快门）
  │     └─ 剩余张数递减 → 底片齿孔计数器同步
  │
  └─ 取出胶卷
        ├─ 未连接 → 弹窗「相机未连接」
        ├─ 无照片 → 弹窗「还没有照片」
        ├─ 电量 < 20% 未充电 → 弹窗「电量不足」
        ├─ 未拍完 → 弹窗确认「提前取出？」
        └─ 选择冲扫方式
              ├─ 即时冲扫 → [传输页] 逐张传输 → [胶卷详情] 逐张显影动画
              └─ 经典冲扫 → [传输页] 逐张传输 → [暗房] 倒计时等待

[暗房]
  └─ 胶卷列表（按时间倒序）
        └─ 点胶卷 → [胶卷详情]
              ├─ 点照片 → [照片查看] 放大 / 设封面 / 保存
              ├─ 分享 → [拼贴分享] 自动排版 → 保存带 AiKen 水印
              ├─ 重命名
              └─ 多选删除

[设置]
  ├─ 冲扫方式：每次询问 / 即时冲扫 / 经典冲扫
  ├─ 自动保存：传输后自动保存到手机相册
  ├─ 日期戳：在成片右下角印拍摄日期
  └─ 打印机配对 → [打印机配对] → [打印机详情] 解绑
```

---

## 六、软硬件通信机制

### 6.1 硬件 → 软件（Props 回调）

| 回调 | 触发时机 | 软件响应 |
|------|----------|----------|
| `onHardware(state)` | 开闸/关闸/拍摄/复位/充电 | 更新 UI 状态（连接状态、张数、电量等） |
| `onShot({ ts })` | 快门拍摄 | 从照片库抽取一张真实照片加入已拍列表 |
| `onReset()` | 硬件复位完成 | 清空已拍照片列表，Toast 提示 |

### 6.2 软件 → 硬件（apiRef 控制接口）

| 方法 | 用途 | 调用时机 |
|------|------|----------|
| `finishRoll()` | 快速拍完一卷（count = 0） | 演示用 |
| `resetFilm()` | 传输成功后重置 count = 36 | 传输完成 |
| `holdAwake()` | 传输期间禁止自动关机 | 开始传输 |
| `rearmSleep()` | 传输结束后重启关机计时 | 传输完成（关闸状态） |
| `setShutterLock(locked)` | 传输期间锁定/解锁快门 | 开始/结束传输 |
| `setCount(remaining)` | 底片齿孔交互同步 | 点击齿孔跳转 |

### 6.3 关键时序

**传输流程**：
1. 用户确认取出 → `holdAwake()` + `setShutterLock(true)`
2. 逐张传输（0.35 秒/张）→ 检测连接状态
3. 传输完成 → 建卷 → `resetFilm()` + `setShutterLock(false)` + `rearmSleep()`
4. 传输中断 → 解锁快门 + 重启关机计时，机内文件保留

---

## 七、关键业务规则

| 规则 | 说明 |
|------|------|
| **关闸锁定快门** | 关闸时快门几乎按不动，仅发出锁定音效 |
| **传输中锁定快门** | 机内文件导出期间禁止拍摄 |
| **传输中禁止自动关机** | 防止传输中途断电 |
| **传输前低电检查** | 电量 < 20% 且未充电时弹窗警告 |
| **传输前连接检查** | 相机未连接时弹窗提示 |
| **无照片禁止取出** | 未拍摄任何照片时取出按钮不可用 |
| **提前取出确认** | 未拍完时弹窗确认，放弃后续拍摄 |
| **开闸自动连接** | 开闸后 1.3 秒自动连接米家 |
| **取出需先选风格** | 未选风格时取出按钮禁用 |
| **复位清空机内记录** | 硬件复位后同步清空 App 已拍列表 |
| **经典冲扫倒计时** | 3~24 小时随机等待（演示加速约 7~58 秒），模拟冲扫仪式感 |
| **传输中断保留文件** | 连接断开即中止传输，照片完整保留在相机内 |
| **卷尽必须冲扫** | 剩余 0 张时取出按钮变为「取出并冲扫」 |
| **低电屏显交替** | 电量 ≤15% 时 LO 与数字 1 秒交替显示 |
| **日期戳** | 显影完成后在成片右下角印 YY MM DD 格式日期 |
| **仅缩放不可编辑** | 照片查看页仅支持缩放，不支持裁剪或滤镜调整 |

---

## 八、风格与成像

### 8.1 风格列表

| ID | 名称 | 胶卷 | 类型 | ISO | 特点 |
|----|------|------|------|-----|------|
| gold200 | 温暖午后 | Kodak Gold 200 | 彩色负片 | 200 | 暖调浓郁，金黄琥珀色，高饱和怀旧感 |
| portra160 | 人像经典 | Kodak Portra 160 | 彩色负片 | 160 | 肤色自然温润，低反差，颗粒极细 |
| fujic200 | 日系蓝调 | Fujifilm C200 | 彩色负片 | 200 | 清冷通透，偏蓝绿，适合街拍 |
| vista400 | 柏林之冬 | Agfa Vista 400 | 彩色负片 | 400 | 冷调浓郁油润，阴影青蓝，厚重感 |
| hp5 | 经典黑白 | Ilford HP5 Plus | 黑白负片 | 400 | 中性灰阶，颗粒适中，经典人文 |
| neon500t | 霓虹之夜 | Kodak Vision3 500T | 彩色负片 | 500 | 电影感暖调，暗部宽容度高，适合夜景（需解锁） |

### 8.2 成像管线

```
底图 → LUT 色彩映射（.cube 文件）→ CSS 滤镜近似过渡
     → 漏光效果（leak）
     → 暗角（vignette）
     → 颗粒纹理（SVG feTurbulence）
     → 日期戳（可选）
     → 显影遮罩动画
```

- 每张照片按 (seed, styleId) 缓存 LUT 处理结果
- LUT 加载前用 CSS filter 做近似过渡，保证即时反馈

---

## 九、照片库

- 64 张真实摄影样片
- Fisher–Yates 洗牌算法，整堆不重复
- 每卷 36 张，拍完需冲扫后重置

---

## 十、技术栈

- **前端框架**：React 19 + React Native Web
- **3D 渲染**：Three.js（WebGL）
- **构建工具**：Vite 6
- **辅助库**：html2canvas（拼贴导出）
- **音效**：WebAudio API 合成
- **色彩处理**：.cube LUT 三线性插值

---

> 文档基于 `/workspace/aiken-demo/` 源码分析生成，反映当前 demo 版本（0.1.0）的完整软硬件交互设计。
